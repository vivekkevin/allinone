<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link rel="stylesheet" href="/css/login.css"> <!-- Link to the login CSS -->
</head>
<body>
    <div class="header">
        <div class="logo">
            <img src="../img/logo.png" alt="Logo">
        </div>
        <div class="login-link">
            <a href="/register">Register</a>
        </div>
    </div>
    <div class="content">
        <div class="form-container">
            <header>
                <h1>Login</h1>
                <% if (step === 1) { %>
                    <p>Please enter your username and password to log in.</p>
                <% } else if (step === 2) { %>
                    <p>A validation code has been sent to your email. Please enter it below to continue.</p>
                <% } %>
            </header>

            <% if (step === 1) { %>
                <!-- Step 1: Username and Password -->
                <form action="/login" method="POST">
                    <div class="form-group">
                        <label for="username">Username</label>
                        <input type="text" id="username" name="username" placeholder="Username" required>
                    </div>
                    <div class="form-group">
                        <label for="password">Password</label>
                        <input type="password" id="password" name="password" placeholder="Password" required>
                    </div>
                    <button type="submit" class="btn">Login</button>
                </form>
            <% } else if (step === 2) { %>
                <!-- Step 2: Validation Code -->
                <form action="/validate-code" method="POST">
                    <div class="form-group">
                        <label for="code">Validation Code</label>
                        <input type="text" id="code" name="code" placeholder="Enter Validation Code" required>
                    </div>
                    <button type="submit" class="btn">Validate</button>
                </form>
            <% } %>
        </div>
    </div>
</body>
</html>
